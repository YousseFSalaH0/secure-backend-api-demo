const test=require("node:test"),assert=require("node:assert/strict"),server=require("./server");let base;test.before(async()=>{await new Promise(r=>server.listen(0,r));base="http://127.0.0.1:"+server.address().port});test.after(()=>server.close());test("health",async()=>assert.equal((await fetch(base+"/health")).status,200));test("auth",async()=>assert.equal((await fetch(base+"/private")).status,401));test("webhook idempotency",async()=>{const o={method:"POST",headers:{authorization:"Bearer demo-token","idempotency-key":"evt-1"}};assert.equal((await fetch(base+"/webhooks/orders",o)).status,202);assert.equal((await fetch(base+"/webhooks/orders",o)).status,200)});
